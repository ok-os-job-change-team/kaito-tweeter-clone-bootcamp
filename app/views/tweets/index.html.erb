<h1>ツイート一覧</h1>

<% @tweets.each do |tweet| %>
  <hr>
  <p><span>メールアドレス: </span><%= tweet.user.email %></p>
  <p><span>ツイート内容: </span><%=link_to tweet.tweet_content, tweet_path(tweet.id) %></p>
<% end %>

<div class="text-notice">
  <%= flash[:notice] %>
</div>
